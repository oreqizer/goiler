type Query {
    # Fetches an object given its ID
    node(
        # The ID of an object
        id: ID!
    ): Node
    account: Account

    # TODO Account connection
    accounts: [Account] @hasRole(role: ADMIN)
}
